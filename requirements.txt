# Core (installable without network access to model/geo registries):
numpy>=1.26
Pillow>=10
opencv-python-headless>=4.10
PyYAML>=6.0
python-dotenv>=1.0

# Geospatial I/O (required for real GeoTIFF support -- see src/depthwizard/io):
rasterio>=1.3

# Depth model backend (required for real Depth Anything V2 inference -- see
# src/depthwizard/depth/backends/depth_anything.py). Without these installed,
# only the explicit DEMO_FALLBACK path is available.
torch>=2.2
transformers>=4.45
safetensors>=0.4

# Dev / test tooling:
streamlit>=1.37
pytest>=8.0
pytest-cov>=5.0
