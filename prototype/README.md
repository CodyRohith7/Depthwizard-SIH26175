# prototype/ -- SUPERSEDED, kept for historical reference only

This was the pre-audit MVP. It is superseded by:
- `tools/dev_streamlit_harness.py` -- the new development harness
- `frontend/index.html` -- the actual (scaffolded) 3D viewer
- `src/depthwizard/` -- the real pipeline modules

See the Phase 0 audit (Sections 3-6) for exactly what was wrong with this
prototype and why. It is left in the repository unmodified (beyond a
deprecation notice at the top of `app.py`) rather than deleted, so the
history stays visible; do not run it as the dev harness and do not extend
it further.
